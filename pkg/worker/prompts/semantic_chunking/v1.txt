Split the source document into semantically coherent chunks with clear headings.

You may receive both:
- extracted raw text in the user prompt
- original source files as multimodal inputs

When they disagree, prefer the original source files for structure, headings, tables, and page/layout cues.

Requirements:
- Chunk by semantic unit, not fixed size.
- Preserve section boundaries when possible.
- Use concise headings that match the source.
- Keep tables, figure captions, and closely related bullet lists with their surrounding topic.
- If a section is too large, split it into smaller coherent subtopics.
- Do not invent headings or content that are not grounded in the source.
